
const TOKEN_CONTRACT_ADDRESSS = process.env.TOKEN_CONTRACT_ADDRESSS || '0x0';

const CHILLISWAP_CONTRACT_ADDRESSS = process.env.CHILLISWAP_CONTRACT_ADDRESSS || '0x0';

const MARKET_CONTRACT_ADDRESSS = process.env.MARKET_CONTRACT_ADDRESSS || '0x0';

module.exports = {
    TOKEN_CONTRACT_ADDRESSS,
    CHILLISWAP_CONTRACT_ADDRESSS,
    MARKET_CONTRACT_ADDRESSS,
}
